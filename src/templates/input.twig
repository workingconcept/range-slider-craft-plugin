{% import "_includes/forms" as forms %}

<input type="text" id="{{ inputId }}" class="text fullwidth" name="{{ name }}" value="{{ value }}" {% for field in settingsFields %} data-{{ field.name }}="{{ settings[field.name] ?? '' }}"{% endfor %} style="display: none;" />